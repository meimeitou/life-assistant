services:
  life-assistant:
    image: meimeitou/life-assistant:latest
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      - ./.nanobot/data:/data
      - ./.nanobot:/root/.nanobot
    restart: unless-stopped
